{{#if isAlbum}}
  {{> imgButton info=info isAlbum=false}}
  <div class="card-body text-middle">
    <p class="card-title"><strong>{{info.name}}</strong></p>
      <p>{{info.release_date}}</p>
  </div>
{{else}}
    {{> imgButton info=info isAlbum=true}}
    <div class="card-body text-middle">
      <p class="card-title"><strong>{{info.name}}</strong></p>
    </div>
{{/if}}